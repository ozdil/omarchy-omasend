#!/usr/bin/env bash
set -euo pipefail
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
BIN="${DIR}/omasend-engine"

if [[ ! -x "${BIN}" ]]; then
    if [[ -f "${DIR}/target/release/omasend-engine" ]]; then
        install -m 755 "${DIR}/target/release/omasend-engine" "${BIN}"
    elif command -v cargo >/dev/null 2>&1 && [[ -f "${DIR}/Cargo.toml" ]]; then
        (cd "${DIR}" && cargo build --release --locked && install -m 755 target/release/omasend-engine "${BIN}") >/dev/null 2>&1 || true
    fi
fi

if [[ -x "${BIN}" ]]; then
    exec "${BIN}" --status
else
    echo '{"text":"OMASEND: OFFLINE","tooltip":"Engine binary not found (run cargo build --release)","class":"alert"}'
fi
